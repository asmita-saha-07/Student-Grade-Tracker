# Student Grading System

A simple command-line Python program to manage student grades, calculate averages, and generate pass/fail remarks. It allows adding, viewing, deleting, and analyzing student marks for multiple subjects.

## Features

- Add new students with marks in English, Mathematics, and Science.
- View all student grades.
- Retrieve grades for a selected student.
- Remove a student from the system.
- Calculate average grades for each student.
- Check pass/fail status based on a user-defined pass mark.
- Assign overall grades (A-F) based on average scores.

## Requirements

- Python 3.x
- `pandas` library
- `csv` module (built-in with Python)

Install `pandas` if not already installed:
```bash
   pip install pandas
```bash
Clone or download this repository.

Run the program:

python student_grading_system.py

Follow the on-screen menu:

Choose your option:
1. Add New Student
2. View Grades
3. Grade of Selected Student
4. Remove Student
5. Calculate average grade
6. Check for pass or fail
7. Exit
--> 
